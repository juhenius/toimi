apiVersion: apps/v1
kind: Deployment
metadata:
  name: toimi-tools-taidot
  namespace: apps
  labels:
    app: toimi-tools-taidot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: toimi-tools-taidot
  template:
    metadata:
      labels:
        app: toimi-tools-taidot
    spec:
      containers:
        - name: toimi-tools-taidot
          image: ${IMAGE_REGISTRY}/toimi-tools-taidot:latest
          ports:
            - containerPort: 8080
          env:
            - name: OpenAI__ApiKey
              valueFrom:
                secretKeyRef:
                  name: toimi-secrets
                  key: openai-api-key
            - name: Qdrant__Host
              value: qdrant.data.svc.cluster.local
            - name: Qdrant__Port
              value: "6334"
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 5
